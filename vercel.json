{
  "installCommand": "cd citylink && npm install",
  "buildCommand": "cd citylink && npm run build",
  "outputDirectory": "citylink/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
